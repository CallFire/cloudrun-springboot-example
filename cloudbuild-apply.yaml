timeout: 7200s
# https://cloud.google.com/cloud-build/docs/configuring-builds/substitute-variable-values
substitutions:
  _POLICY_REPO: '' # add path to policies here https://github.com/forseti-security/policy-library/blob/master/docs/user_guide.md#how-to-use-terraform-validator
steps:
  # - name: "gcr.io/cloud-builders/gcloud"
  #   args: ["auth", "list"]

  # - name: "gcr.io/cloud-builders/docker"
  #   args: ["build", "-t", "gcr.io/${PROJECT_ID}/cloudrun-example", "."]

  # - name: "gcr.io/cloud-builders/docker"
  #   args: ["push", "gcr.io/${PROJECT_ID}/cloudrun-example"]

  # Uses 195303305359@cloudbuild.gserviceaccount.com
  - name: "gcr.io/cloud-builders/gcloud"
    args: ["beta", "run", "deploy", "my-springboot-service", "--image", "gcr.io/${PROJECT_ID}/cloudrun-example:latest", "--platform", "managed", "--region", "${_DEFAULT_REGION}"]

# images:
#   - "gcr.io/$PROJECT_ID/deploy/cloudrun-example"
